현재 pytest 테스트가 작성되어 있어.
`notes.note_service.NoteService.get_note_by_id` 메서드만 구현해서
현재 failing test를 통과하도록 해줘.

조건:
- 테스트 코드는 수정하지 말아줘.
- 기존 시그니처는 유지해줘.
- 최소 구현으로 테스트를 통과시키는 것이 목표다.
- 아직 리팩터링은 하지 말아줘.

계약:
- `note_id`가 존재하면 해당 `Note`를 반환한다.
- 존재하지 않으면 `None`을 반환한다.
- 초기 노트 목록은 생성자에서 `initial_notes`로 주입받을 수 있다.

추가 조건:
- in-memory list를 순회하는 단순한 방식으로 구현해도 된다.
- 최종 출력은 수정된 프로덕션 코드만 보여줘.